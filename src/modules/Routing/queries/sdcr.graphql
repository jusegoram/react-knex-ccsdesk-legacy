query routing($activityNumber: String) {
  routing(activityNumber: $activityNumber) {
    cid
    techId
    firstName
    lastName
    subcontractor
    distance
    hasSkill
    workingThatDay
    hasTimeslotFree
  }
}
