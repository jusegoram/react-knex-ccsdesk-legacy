query routing($activityNumber: String) {
  routing(activityNumber: $activityNumber) {
    techId
    firstName
    lastName
    subcontractor
    distance
    hasSkill
    workingThatDay
    hasTimeslotFree
  }
}
