#import "../Tech.graphql"

query tech($cid: String!) {
  tech(cid: $cid) {
    ...TechProps
    groupNames
    groups
    contacts {
      id
      firstName
      lastName
      role
      phoneNumber
    }
  }
}
